{
  "name": "remodex-relay",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "node ./server.cjs",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "wrangler": "^4.57.1"
  }
}
