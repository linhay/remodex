# 2026-03-15

## 关键决策
- 从今天开始与上游分叉，进入本地分支迭代模式。
- 初始化记忆系统目录与主记忆文件（MEMORY.md + memory/）。

## 当日行动
- 完成 chats 首屏性能优化：
  - 连接后先拉 threads，再拉 models。
  - listThreads 首屏默认 limit=20，并后台回填。
  - Sidebar 分组重建去抖。
  - Sidebar 指标采用“可见优先、延后补算”。
- 按 `swiftui-view-refactor` 将页面状态进一步下沉为 `@Observable`：
  - `SidebarView` 抽出 `SidebarViewModel`，承接搜索、分组重建、指标回填、弹窗状态与创建会话状态。
  - `QRScannerView` 抽出 `QRScannerViewModel`，承接权限/手动输入相关本地状态。
  - 新增 ViewModel 单测覆盖关键状态逻辑（筛选、可创建门禁、手动输入提交门禁）。
  - 为新测试补充 Xcode target 收录（`project.pbxproj`）。
  - 处理单测进程在对象回收阶段的宿主崩溃：测试内保留 `@Observable` ViewModel 与 `CodexService` 引用，避免 `pointer being freed was not allocated`（仅测试侧规避，不影响生产逻辑）。
- 设置页连接信息新增当前域名显示：
  - 新增 `SettingsConnectionDomainFormatter`，从 relay URL 提取 host，异常/空值回落为 `not set`。
  - `SettingsView` 的 Connection 区块新增 `Domain: ...` 行。
  - 新增 `SettingsConnectionDomainFormatterTests` 并接入测试 target。
- 修复设置按钮连续点击导致重复导航：
  - `ContentView` 中 settings 路由追加改为幂等门禁（`showSettings == true && navigationPath.isEmpty` 才 append）。
  - 新增 `ContentSettingsNavigationGate` 纯逻辑门禁，并补充对应单测覆盖空路径、非空路径、返回后再次进入场景。

## 风险与说明
- 当前仓库含有未提交改动，后续提交需按功能拆分，避免混入无关变更。
