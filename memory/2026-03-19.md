## 00:40 四个崩溃用例逐个定位与回归
- 背景：用户要求“逐个定位 4 个崩溃用例”，并在模拟器上完成回归。
- 用例状态（逐个单跑）：
  - `CodexPushNotificationRegistrationTests.testMissingNotificationTargetStaysPendingWhenOnlyThreadListOmitsIt`：通过。
  - `CodexMobileUITests.testTapAccountPushesToChatScreen`：初始失败，根因是 UI Test 用英文文案 `"Menu"` 查找按钮，在中文环境不稳定。
  - `CodexMobileUITests.testTapAccountDoesNotBounceBackToAccountPage`：同上失败根因（`"Menu"` 文案定位）。
  - `CodexMobileUITests.testCaptureSettingsAccountListScreenshots`：初始失败，卡在 `settingsApp.swipeUp()` 触发 UI idle 超时。
- 修复决策：
  - 在聊天页菜单按钮新增稳定 identifier：`chat.menu.button`（避免本地化文案差异）。
  - 两个账号跳转用例改为基于 identifier 定位。
  - 截图用例移除不稳定的全局 `swipeUp()` 步骤。
- 回归结果：
  - 两个账号跳转用例通过；截图用例通过（耗时较长但无失败）；通知注册用例通过。

## 01:00 回灌后恢复「项目分组默认 5 条折叠」
- 现象确认：`main` 回灌后，Sidebar 项目分组内会话列表恢复为全量展开，丢失了“默认仅显示前 5 条 + 展开/收起”的行为。
- 追溯依据：历史提交 `483e735`（`feat: limit project chats to 5 with expand toggle`，2026-03-15 14:18:28 +0800）。
- TDD 过程：
  - 先补失败测试：`SidebarThreadGroupingTests`
    - `testProjectThreadPreviewStateShowsFirstFiveByDefault`
    - `testProjectThreadPreviewStateShowsAllWhenExpanded`
  - 首次执行测试红灯：`cannot find 'SidebarProjectThreadPreviewState' in scope`。
  - 最小实现恢复：在 `SidebarThreadListView` 重新引入
    - `expandedProjectThreadPreviewGroupIDs`
    - `SidebarProjectThreadPreviewState.previewLimit = 5`
    - `visibleThreads(...)` / `shouldShowExpandToggle(...)`
    - `Show N more` / `Show less` 切换按钮
- 回归结果：
  - `xcodebuild -project CodexMobile/CodexMobile.xcodeproj -scheme CodexMobile -destination 'id=17123E36-E2BB-4992-AB89-079D0EE499DF' -only-testing:CodexMobileTests/SidebarThreadGroupingTests test`
  - 17 tests, 0 failures（`** TEST SUCCEEDED **`）。

## 01:40 局域网连接固定为本地优先
- 问题：bridge 在 `REMODEX_RELAY` 为云端时默认先连云，手机可能先连本地，触发 cloud/local split（表现为局域网看似连不上）。
- 处理：运行态已重启为本地优先（`linhey.local`、LAN IP 在前，云端回退在后）。
- 永久化：`run-local-remodex.sh` 生成 `REMODEX_RELAY_CANDIDATES` 改为本地优先顺序，避免重启后回到云端优先。

## Runtime/Pairing 噪声修复（Settings + Transport）
- 现象：Settings 页面 Reasoning Picker 在缓存值为 `medium` 且当前模型不支持该值时出现 "selection invalid" 警告；配对阶段出现 `NWConnection waiting(ECONNABORTED)` 噪声日志。
- 决策：在 UI 侧增加选择值合法化兜底（不在可选列表则回退 Auto），并在 NWConnection 状态日志中抑制 benign disconnect（含 `ECONNABORTED`）相关 waiting/failed 输出。
- 验证：新增 `TurnComposerMetaMapperTests` 及 `CodexServiceConnectionErrorTests` 覆盖兜底与日志抑制判断。
- 编译阻塞修复：`CodexService+Transport` 的 `NWConnection.stateUpdateHandler` 闭包处触发 MainActor 隔离编译错误（非隔离上下文调用 actor-isolated 方法）。
- 处理：将日志抑制判断改为 `nonisolated` 纯函数路径，并补 `@Sendable` 到并发执行的本地 `finish` 闭包；重新执行 xcodebuild 后 `BUILD SUCCEEDED`。
- 测试补全：发现 `TurnComposerMetaMapperTests.swift` 未加入 `CodexMobileTests` target，已更新 `project.pbxproj` 绑定到 Sources。
- 回归：定向执行 `CodexServiceConnectionErrorTests` + `TurnComposerMetaMapperTests`，共 14 条通过（0 fail）。
